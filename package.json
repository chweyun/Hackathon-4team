{
  "name": "react-chat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd ./react-chat-client && npm run start\" \"cd ./react-chat-server && npm run dev\"",
    "build": "react-scripts build",
    "start": "NODE_PATH=src react-scripts start",
    "heroku-postbuild": "npm run build"
  },
  "engines": {
    "node": "16.15.0",
    "npm": "8.15.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "react-scripts": "^5.0.1",
    "styled-components": "^5.3.5"
  }
}
